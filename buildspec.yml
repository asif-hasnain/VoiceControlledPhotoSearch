version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7

  build:
    commands:
      - sam build

  post_build:
    commands:
      - sam package --output-template-file packaged.yaml --s3-bucket test-assignment3

artifacts:
  type: zip
  files:
    - packaged.yaml